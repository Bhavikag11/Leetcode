class Solution {
public:
    char findTheDifference(string s, string t) {
       int a=0,b=0;
        for(int i=0;i<s.size();i++)
        {
            a+=s[i];
        }
        for(int i=0;i<t.size();i++)
        {
            b+=t[i];
        }
        return b-a;
    }
};
